const number1 = 1;
const number2 = 2;
const product = number1 * number2;
console.log(`Value of product: `, product);
console.log('typerof product: ', typeof product);

const charge = 12;
const payment = 10;
const amountRemaining = charge - payment;
console.log(`Value of amountRemaining: `, amountRemaining);
console.log(`Typeof amountRemaining: `, typeof amountRemaining);

const test = 5;
const assignments = 15;
const final = 20;
const grade = (test + assignments + final) / 3;
console.log(`Value of grade: `, grade);
console.log(`typeoff grade: `, typeof grade);

const firstName = `Ragad`;
const lastName = 'Mohammed';
const fullName = firstName + ' ' + lastName;
console.log(`Value of fullName:`, fullName);
console.log(`typeoff fullName:`, typeof fullName);

const pH = 5;
const isAcidic = pH < 7;
console.log(`Value of isAcidic: `, isAcidic);
console.log(`tyepof isAcidic: `, typeof isAcidic);

const numberOfSoliders = 200;
const isSparta = numberOfSoliders > 300;
console.log(`Value of isSparta: `, isSparta);
console.log(`TypeOff isSparata: `, typeof isSparta);

let nickName = fullName;
nickName += ' is the GOAT';
console.log(`value of Nickname: `, nickName);
console.log(`typeOff nickName: `, typeof nickName);
